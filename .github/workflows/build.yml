name: build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Validate scripts and plists
        run: ./scripts/check.sh
      - name: Swift self-tests
        run: swift run -c debug --package-path swift Parakey --self-test all
      - name: Build app bundle
        run: ./scripts/build-app.sh "$RUNNER_TEMP/ABX Voice Assist.app"
      - name: Verify app bundle
        run: |
          test -x "$RUNNER_TEMP/ABX Voice Assist.app/Contents/MacOS/ABXVoiceAssist"
          codesign --verify --deep --strict "$RUNNER_TEMP/ABX Voice Assist.app"
      - name: Exercise installer and uninstaller
        run: |
          mkdir -p "$RUNNER_TEMP/release"
          ditto -c -k --sequesterRsrc --keepParent \
            "$RUNNER_TEMP/ABX Voice Assist.app" "$RUNNER_TEMP/release/ABX Voice Assist.zip"
          checksum="$(shasum -a 256 "$RUNNER_TEMP/release/ABX Voice Assist.zip" | awk '{print $1}')"
          ABX_VOICE_ASSIST_RELEASE_URL="file://$RUNNER_TEMP/release/ABX Voice Assist.zip" \
          ABX_VOICE_ASSIST_RELEASE_SHA256="$checksum" \
          ABX_VOICE_ASSIST_NO_OPEN=1 \
            ./install.sh
          test -x /Applications/ABX Voice Assist.app/Contents/MacOS/ABXVoiceAssist
          codesign --verify --deep --strict /Applications/ABX Voice Assist.app
          ./uninstall.sh
          test ! -e /Applications/ABX Voice Assist.app
